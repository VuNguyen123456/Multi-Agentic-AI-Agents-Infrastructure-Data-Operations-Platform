{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" }, "unit": "short", "min": 0 },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "options": {
        "barRadius": 0.1,
        "barWidth": 0.8,
        "legend": { "displayMode": "list", "placement": "bottom", "showLegend": true },
        "orientation": "horizontal",
        "showValue": "always",
        "stacking": "none"
      },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "Prometheus" },
          "expr": "sum by (status) (increase(infra_recovery_runs_total[$__range]))",
          "instant": true,
          "legendFormat": "{{status}}",
          "range": false,
          "refId": "A"
        }
      ],
      "title": "Recovery runs (total in range)",
      "type": "barchart"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "options": { "colorMode": "value", "graphMode": "area", "reduceOptions": { "calcs": ["lastNotNull"] } },
      "targets": [{ "expr": "infra_recovery_pending_approvals", "refId": "A" }],
      "title": "Pending approvals",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" }, "unit": "short", "min": 0 },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 3,
      "options": {
        "orientation": "horizontal",
        "showValue": "always",
        "legend": { "showLegend": true, "displayMode": "list", "placement": "bottom" }
      },
      "targets": [
        {
          "expr": "sum by (decision) (increase(infra_recovery_human_decisions_total[$__range]))",
          "instant": true,
          "legendFormat": "{{decision}}",
          "range": false,
          "refId": "A"
        }
      ],
      "title": "Human decisions (total in range)",
      "type": "barchart"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" }, "unit": "short", "min": 0 },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 4,
      "options": {
        "orientation": "horizontal",
        "showValue": "always",
        "legend": { "showLegend": true, "displayMode": "list", "placement": "bottom" }
      },
      "targets": [
        {
          "expr": "sum by (agent) (increase(infra_recovery_agent_steps_total{agent!=\"__interrupt__\"}[$__range]))",
          "instant": true,
          "legendFormat": "{{agent}}",
          "range": false,
          "refId": "A"
        }
      ],
      "title": "Agent steps (total in range)",
      "type": "barchart"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 16 },
      "id": 5,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        {
          "expr": "histogram_quantile(0.50, sum by (le, endpoint) (rate(infra_recovery_graph_run_seconds_bucket[$__range])))",
          "legendFormat": "p50 {{endpoint}}",
          "refId": "A"
        },
        {
          "expr": "histogram_quantile(0.95, sum by (le, endpoint) (rate(infra_recovery_graph_run_seconds_bucket[$__range])))",
          "legendFormat": "p95 {{endpoint}}",
          "refId": "B"
        }
      ],
      "title": "Graph run latency (p50 / p95)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "short", "min": 0 }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 16 },
      "id": 6,
      "options": { "orientation": "horizontal", "showValue": "always" },
      "targets": [
        {
          "expr": "sum by (endpoint) (increase(infra_recovery_api_errors_total[$__range]))",
          "instant": true,
          "legendFormat": "{{endpoint}}",
          "range": false,
          "refId": "A"
        }
      ],
      "title": "API errors (total in range)",
      "type": "barchart"
    }
  ],
  "refresh": "15s",
  "schemaVersion": 39,
  "tags": ["infra-recovery", "prometheus"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "Agent Recovery — Metrics",
  "uid": "agent-recovery-metrics",
  "version": 3
}
