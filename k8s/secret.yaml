apiVersion: v1
kind: Secret
metadata:
  name: infra-recovery-secrets
  namespace: default
type: Opaque
stringData:
  # Put your real values here
  # stringData lets you write plain text — K8s base64-encodes it automatically
  ANTHROPIC_API_KEY: "sk-ant-REPLACE_WITH_YOUR_KEY"
  POSTGRES_PASSWORD: "password"